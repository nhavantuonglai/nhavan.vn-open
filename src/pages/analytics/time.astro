---
import Layout from '~/layouts/page.astro';

import Time from '~/components/content/analytics/time.astro';

import Action from '~/components/content/form.astro';

const metadata = {
	title: 'Bây giờ là mấy giờ mấy phút mấy giây?',
	description: 'Bây giờ là mấy giờ mấy phút mấy giây hiển thị giờ hiện tại chính xác đến từng giây, giúp bạn dễ dàng theo dõi, cập nhật thời gian mọi lúc, mọi nơi.',
};
---

<Layout metadata={metadata}>

	<Time/>

	<Action/>

</Layout>
